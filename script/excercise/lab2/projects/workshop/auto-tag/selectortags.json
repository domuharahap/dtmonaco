{
  "name": "{{ .name }}",
  "description": null,
  "rules": [],
  "entitySelectorBasedRules": [
    {
      "enabled": true,
      "entitySelector": "{{ .selector }}",
      "valueFormat": "{{ .format }}"
    }
  ]
}
